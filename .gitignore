# 사용자별 설정 (시크릿/개인 경로 포함) — 절대 커밋 금지
config.env
.assemblyai_key
.hf_token

# WhisperX 전용 venv (선택 기능, 로컬 생성)
.venv-whisperx/

# 런타임 상태 (개인 녹음 ID, 락, 실패 카운트)
state/
# v1 레거시 위치 (자동 마이그레이션 전 잔재)
pulled_ids.txt
skipped_ids.txt
.lock

# 로그
logs/

# whisper 모델 (~1.5GB, 설치 시 다운로드)
models/
*.bin

# 오디오 (개인 녹음)
*.mp3
*.m4a
*.wav
*.aac
*.opus
*.flac

# OS / 에디터
.DS_Store
__pycache__/
*.pyc

# 도구 아티팩트 (oh-my-claudecode 등)
.omc/
